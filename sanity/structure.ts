// Desk structure configuration for Sanity Studio
// This file is reserved for future use with structureTool integration
// For now, Sanity uses the default desk structure based on schema types

export const deskStructure = null;
